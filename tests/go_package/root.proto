// A protobuf in the root of a workspace that has the go_package option set.

syntax = "proto3";

option go_package = "github.com/pubref/rules_protobuf/tests/go_package/root";

message Dir {
  string name = 1;
}
